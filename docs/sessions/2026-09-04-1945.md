# Session 2026-09-04 19:45

> **Closed:** 2026-09-04T19:45:00-03:00
> **Branch:** main
> **Commits in this session:** 9, `56031d2..HEAD`, plus the close commit

## TL;DR

The product exists: 106 tests green, 15 mutations red, three containers healthy on this machine, the GPU image reads its GPU, and the harness runs on its own repository.

## What happened

- Read both sources in full: 14 files of source A (ace-techtiva) and 12 of source B (snake-outbreak).
- Wrote `ARCHITECTURE.md` and `docs/PROPOSALS.md` before the code (`56031d2`).
- Built the package, standard library only: 16 modules (`b04a6f8`).
- Wrote the 39 files that `init` installs (`0ca7473`), and the test suite (`5d493c9`).
- Ran `init` on this repository and on a scratch repository with its own `CLAUDE.md` (`e91cd5a`).
- Fixed three defects that the runs found (`9fb824e`): two services built one image tag in parallel, the Archive table header was read as a front, and a 0-chunk index printed `?%`.
- Recorded the mutation proof in `docs/MUTATION.md` (`e76634a`), and seeded the product's own sprint (`0f656c6`).
- Started `TASK-0001` after `nvidia-smi -L` printed the RTX 3070 Ti from inside the GPU image. It awaits the user's glance.

## Decisions

- Ids carry no epic (`TASK-0001` is global). Source B stored the epic in the id and in the folder. The folder is the truth.
- The companion of the index is Python, not JavaScript. One language, one test runner.
- The rules template goes to `CLAUDE.md` when none exists, else to `.claude/rules/harness.md`. One source, two destinations, recorded in the manifest.
- The product targets Python 3.10, not 3.11. This machine runs 3.10.12. The code avoids `datetime.UTC`, `tomllib`, and `fromisoformat` with `Z`.
- Ports 8410, 8411, 8412. Ports 8181 and 8182 are taken here by the stack of source A.

## Repository state at close

```text
clean after the close commit; .harness/env.local and .harness/board.sqlite are git-ignored
```

## Open items

- `TASK-0001` (in progress, GLANCE): the user reads the `nvidia-smi` line in the task file and closes it with `done --verdict`.
- `TASK-0002` (user): macOS was not measured. `ports.py` falls back to `lsof` there, untested.
- `TASK-0003` (user): the push. The commits wait locally.
- The RAG stack: the first CPU embed of 64 documents took over 15 minutes. The GPU target is the fix, and it ran only as `nvidia-smi`, not as the stack.
- `memory` collection: the memory directory of this repository is empty, so the canary reports `not indexed yet` until a session writes a memory.

## References

- `docs/DESIGN-LAWS.md`, `docs/PROPOSALS.md`, `docs/MUTATION.md`, `docs/HOOKS.md`
- `infra/rag/README.md` for the read-only proof
- Source A: `~/external-work/techtiva/repos/ace-techtiva/.claude/skills/session-start/SKILL.md`
- Source B: `/mnt/e/snake-outbreak/files/sprints/sprints.md`

## How to resume

1. `python3 -m harness session open`.
2. Read the report of 2026-09-04 and decide the push (`TASK-0003`).
3. If the GPU line satisfies you, close `TASK-0001`: `python3 -m harness done TASK-0001 --verdict "..." --by user`.
4. Run the suite: `python3 -m unittest discover -s tests -t .`.
