# Roadmap — harness-rag-pipeline

The user writes this file. It holds the long horizon: the phases, the order, and
the reasons. It holds no status and no count. The board computes those.

## Horizon

| phase | what | why now | source |
|---|---|---|---|
| 1 | (the first phase) | (the measurement or the request behind it) | (a document or a quote) |

## Decisions that are open

| id | question | what changes with each answer |
|---|---|---|
| D1 | (the question) | (the consequence) |

A task that waits for one of these names it in `needs-decision`. `next` skips it and
reports the decision.
