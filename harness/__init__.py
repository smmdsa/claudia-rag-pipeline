"""The harness: an agent pipeline and a folder board for any repository.

The package uses the Python standard library only. `tests/test_policy.py`
fails if a module imports a third-party package.
"""

VERSION = "0.2.0"
MIN_PYTHON = (3, 10)
